About This Site
===============================================================================

The site is built from the content of this project here using ``captura-text``. 
To see the building specification inspect ``text.yaml``. To use with 
``captura-text``, just clone this into the ``docs`` dir.
